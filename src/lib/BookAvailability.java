package lib;

/**
 * This enum is used to show the availability status of a book.
 * It has two objects(AVAILABLE, BORROWED) that represent the availability status of a book
 */
public enum BookAvailability {
    AVAILABLE, BORROWED;
}

